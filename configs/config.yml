# configs/config.yml
configs:
  database:
    postgres:
      host: localhost
      port: 5432
      username: your_username
      password: your_password
      database: your_db_name
      synchronize: false  # 生产环境务必 false，用迁移管理
      logging: true